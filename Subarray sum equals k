class Solution:
    def subarraySum(self, nums: List[int], k: int) -> int:
        prefix_sum_cnt=defaultdict(int)
        prefix_sum_cnt[0]=1
        prefix_sum=0
        cnt=0
        for num in nums:
            prefix_sum+=num
            if(prefix_sum-k)in prefix_sum_cnt:
                cnt+=prefix_sum_cnt[prefix_sum-k]
            prefix_sum_cnt[prefix_sum]+=1
        return cnt
